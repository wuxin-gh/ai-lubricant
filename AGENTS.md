# Agent Instructions

本文件是本仓库的项目级工作准则，用于约束 OpenCode / OMO / 其他 agent 在本项目中的行为。

## 沟通语言

- 默认使用中文与用户沟通。
- 除非用户明确要求英文，否则说明、计划、问题、总结、风险提示都使用中文。
- 代码、命令、错误信息、API 名称、文件路径、配置字段等保持原文，不强行翻译。

## 知识库与文档

本项目的长期知识、风险、历史经验和架构说明应优先沉淀到文档中，而不是只停留在一次性对话里。

常见文档位置：

| 类型 | 文档 |
|---|---|
| 产品定位、核心功能、路线图、设计取舍 | `PLAN.md` |
| 架构、模块职责、数据流、协议、数据库、运行模式 | `WHITEPAPER.md` |
| 新功能、跨模块修复、高风险修复、多轮进度 | `docs/{feature-name}.md` |
| 高风险逻辑、兼容点、事故经验 | `docs/RISK_REGISTER.local.md` / `docs/MISTAKES.local.md` |
| 实现细节、注释、测试、脚本 | 对应源码 |

## 大变动或高风险任务前必须查阅文档

当任务涉及以下情况时，开始修改前必须先读取相关文档，用来验证理解、确认历史约束，并避免破坏已有设计：

- 大范围重构或跨模块改动
- 新功能设计或核心流程变更
- 鉴权、认证、密钥、token、计费相关逻辑
- 协议转换、路由、模型选择、渠道选择、fallback
- 数据库、缓存、并发、生产配置
- 日志脱敏、安全边界、隐私数据
- 历史兼容行为不明确
- 对现有架构或业务不变量没有把握

优先检查：

1. `PLAN.md`
2. `WHITEPAPER.md`
3. `docs/RISK_REGISTER.local.md`
4. `docs/MISTAKES.local.md`
5. 与当前任务相关的 `docs/{feature-name}.md`

如果相关文档不存在或信息不足，需要在回复中明确说明“不存在相关文档”或“文档未覆盖该问题”，不要假装已经确认。

## 文档更新原则

完成较大变动或高风险修改后，需要判断是否应更新文档。

需要更新文档的情况：

- 新增或改变了架构、模块职责、数据流、协议、运行模式
- 新增或改变了核心业务不变量
- 新增或改变了风险点、兼容点、验证方式
- 发现历史文档过期、错误或缺失
- 发生误判需求、误解历史逻辑、改错范围、引入回归等问题

无需更新文档的情况：

- 单点 bugfix
- 纯测试补充
- 注释或格式调整
- 无行为变化的内部小改动

如果无需更新文档，需要在最终汇报中说明原因。

## 修改后汇报

每次完成文件修改后，需要向用户说明本次改动内容。说明应包括：

- 修改了哪些文件
- 每个文件的主要改动
- 是否涉及高风险逻辑或历史兼容
- 是否读取或更新了相关文档
- 是否执行了验证；如果未验证，需要说明原因

说明保持简洁，不堆砌无关细节。

## 子 agent 状态检查

使用多个子 agent 并行工作时，需要关注每个子 agent 的状态和结果。

- 如果发现某个子 agent 非正常结束、卡住、输出明显不完整或偏离任务，不必等待所有子 agent 都完成。
- 应根据上下文判断是继续同一个子 agent 会话、重新启动该子 agent，还是新开一个子 agent 接手工作。
- 处理异常子 agent 前，需要保留已完成子 agent 的有效结论，避免重复劳动。
- 如果异常会影响整体方案或实现判断，需要先解决该异常或重新分配任务，再继续依赖其结果的工作。

## 分支与 worktree 安全规则

- 未经用户明确允许，禁止在主工作区私自切换分支（包括 `git checkout <branch>`、`git switch <branch>`、隐式 checkout 等）。
- 如果任务需要分支隔离、并行开发、验证其它分支，必须优先创建独立 worktree，在 worktree 内完成工作，不要移动用户当前工作区所在分支。
- 如果确实必须切换当前工作区分支，必须先向用户说明原因、当前分支、目标分支、未提交改动处理方案，并获得明确确认。
- 在多 session 场景下，任何提交、合并、stash、reset、rebase 前都必须先检查 `git status` 和 `git branch --show-current`，避免误操作其它 session 的工作。
- 不要因为实现方便而把主工作区切到临时分支；临时任务使用 `git worktree add` 创建隔离目录。
- 合并代码的时候不要强制合并，如果需要用户确认，可以停下来进行询问。

## 本地隐私文档

以下文档视为本地隐私知识库，默认不要提交、push 或共享：

- `docs/RISK_REGISTER.local.md`
- `docs/MISTAKES.local.md`
- 其他 `docs/*.local.md`

涉及 commit / push / PR 前，需要确认这些文件没有被错误加入 staged files。
